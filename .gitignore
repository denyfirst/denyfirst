# Build output
/denyfirst
/dist/
/bin/
*.exe
*.test
*.out

# Coverage
coverage.out
coverage.html

# Go workspace files — machine-specific, must not be shared
go.work
go.work.sum

# ── Secrets ──────────────────────────────────────────────────────────
# Push protection is the real defence; this is the layer before it.
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.jks
*.keystore
id_rsa
id_ed25519
id_ecdsa
*_rsa
*_ed25519
credentials.json
secrets.yaml
secrets.yml
.netrc
.npmrc

# Editors
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db

# Local scratch — a place to put things that must never be committed
/tmp/
/scratch/
/local/
# Local counter file. Real deployments name their own path.
stats.json
stats.json.tmp
dist/